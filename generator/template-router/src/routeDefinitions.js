// Returns routes here: [{name, component, menu}...]
// name: display name of the route
// component: the corresponding component
// optional: menu.icon: icon for navigation

const routeDefinitions = [
/*   { name: "main", component: main, menu: { icon: "fa-television" } },
  { name: "about", component: about, menu: { icon: "info" } } */
];

export default routeDefinitions;
